#!/usr/bin/env node
// scripts/seed-demo.mjs — populate a demo vault + fake Hive Mind clone with
// NDA-safe seed data, so screenshots and demo recordings have realistic content.
//
// Implementation lands with the `demo_seed` todo.

console.log("[smithers] demo seed (stub) — see templates/seed-data/");
process.exit(0);
