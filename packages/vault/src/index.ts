// @smithers/vault — typed read/write helpers for the user's markdown vault.
//
// Implementation lands as part of the `vault_layer` todo. This stub keeps the
// package importable and the workspace graph wired so the web app can already
// reference it.

export const VAULT_PACKAGE_VERSION = "0.0.1";

export type ProjectKind = "partner" | "team" | "personal";
